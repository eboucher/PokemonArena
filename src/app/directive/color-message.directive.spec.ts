import { ColorMessageDirective } from './color-message.directive';

describe('ColorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
